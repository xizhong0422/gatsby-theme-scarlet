{
  "private": true,
  "name": "gatsby-theme-scarlet-demo",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "gatsby clean && gatsby build",
    "develop": "gatsby develop -o",
    "format": "prettier --write src/**/*.{js,jsx}",
    "start": "gatsby clean && npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\""
  },
  "dependencies": {
    "gatsby": "^2.17.7",
    "gatsby-theme-scarlet": "*",
    "react": "^16.11.0",
    "react-dom": "^16.11.0"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
